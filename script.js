(()=>{const home=document.querySelector('#home'),ritual=document.querySelector('#ritual'),drawer=document.querySelector('#drawer'),shade=document.querySelector('#shade'),menu=document.querySelector('#menuToggle'),count=document.querySelector('#count'),range=document.querySelector('#range'),fireStage=document.querySelector('#fireStage'),canvas=document.querySelector('#fireCanvas'),toast=document.querySelector('#toast');const ctx=canvas.getContext('2d',{alpha:true});let energy=+range.value/100,particles=[],last=0;const punishments=['Щоб Wi-Fi ловив тільки біля роутера','Щоб кава завжди остигала за 30 секунд','Щоб зарядка зникала саме коли треба','Щоб будильник дзвонив у вихідний','Щоб після прання губилась одна шкарпетка'];function resize(){const r=canvas.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);canvas.width=Math.max(1,Math.round(r.width*d));canvas.height=Math.max(1,Math.round(r.height*d));ctx.setTransform(d,0,0,d,0,0)}function spawn(w,h){const strong=.18+energy*.82;const ring=Math.random()<.55;const a=Math.random()*Math.PI*2;const rad=ring?(w*.19+Math.random()*w*.13):(Math.random()*w*.24);const x=w*.5+Math.cos(a)*rad;const y=h*.66+Math.sin(a)*rad*.58;particles.push({x,y,vx:(Math.random()-.5)*(10+35*strong),vy:-(34+Math.random()*(55+95*strong)),life:0,max:.42+Math.random()*(.55+.45*strong),size:5+Math.random()*(9+18*strong),phase:Math.random()*6.28})}function draw(t){const r=canvas.getBoundingClientRect(),w=r.width,h=r.height;if(!w||!h){requestAnimationFrame(draw);return}if(canvas.width===0)resize();const dt=Math.min(.035,(t-last)/1000||.016);last=t;ctx.clearRect(0,0,w,h);const amount=Math.floor((2+energy*10)*(dt*60));for(let i=0;i<amount;i++)spawn(w,h);particles=particles.filter(p=>{p.life+=dt;if(p.life>=p.max)return false;p.x+=p.vx*dt+Math.sin(p.phase+p.life*13)*(5+energy*8)*dt;p.y+=p.vy*dt;p.vy-=8*dt;const q=p.life/p.max,fade=Math.sin(Math.PI*Math.min(1,q));const s=p.size*(1-q*.38)*(1+energy*.35);const g=ctx.createRadialGradient(p.x,p.y+s*.12,0,p.x,p.y,s);const alpha=fade*(.28+energy*.72);g.addColorStop(0,'rgba(255,248,190,'+alpha+')');g.addColorStop(.18,'rgba(255,190,45,'+alpha+')');g.addColorStop(.48,'rgba(255,92,0,'+(alpha*.92)+')');g.addColorStop(.74,'rgba(230,25,0,'+(alpha*.48)+')');g.addColorStop(1,'rgba(120,0,0,0)');ctx.save();ctx.translate(p.x,p.y);ctx.rotate(Math.sin(p.phase+p.life*9)*.18);ctx.scale(.7,1.65+energy*.9);ctx.fillStyle=g;ctx.beginPath();ctx.ellipse(0,0,s,s,0,0,Math.PI*2);ctx.fill();ctx.restore();return true});requestAnimationFrame(draw)}function show(x){home.classList.remove('active');ritual.classList.remove('active');x.classList.add('active');close();scrollTo(0,0);setTimeout(resize,30)}function close(){drawer.classList.remove('open');shade.classList.remove('open')}function n(){return parseInt(count.textContent.replace(/\s/g,''),10)||4351}function setN(v){count.textContent=new Intl.NumberFormat('uk-UA').format(v)}function note(t){toast.textContent=t;toast.classList.add('show');clearTimeout(window.t);window.t=setTimeout(()=>toast.classList.remove('show'),2200)}document.querySelector('#goRitual').onclick=()=>show(ritual);document.querySelector('#addEnergy').onclick=()=>{range.value=Math.min(100,+range.value+12);range.dispatchEvent(new Event('input'));setN(n()+1);note('Енергію підкинуто')};range.oninput=()=>{energy=+range.value/100;fireStage.style.setProperty('--energy',energy.toFixed(2));};document.querySelector('#random').onclick=()=>document.querySelector('#punishment').value=punishments[Math.floor(Math.random()*punishments.length)];document.querySelector('#ritualForm').onsubmit=e=>{e.preventDefault();setN(n()+1);note('Ритуал запущено')};menu.onclick=()=>{drawer.classList.toggle('open');shade.classList.toggle('open')};shade.onclick=close;document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>show(b.dataset.go==='home'?home:ritual));addEventListener('resize',resize,{passive:true});range.dispatchEvent(new Event('input'));resize();requestAnimationFrame(draw)})();