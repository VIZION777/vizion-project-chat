(()=>{const $=s=>document.querySelector(s),home=$('#home'),ritual=$('#ritual'),drawer=$('#drawer'),shade=$('#shade'),menu=$('#menuToggle'),count=$('#count'),range=$('#range'),stage=$('#fireStage'),video=$('#fireVideo'),canvas=$('#fireCanvas'),ctx=canvas.getContext('2d'),toast=$('#toast');let energy=+range.value/100,sparks=[];const punishments=['Щоб Wi-Fi ловив тільки біля роутера','Щоб кава завжди остигала за 30 секунд','Щоб зарядка зникала саме коли треба','Щоб будильник дзвонив у вихідний','Щоб після прання губилась одна шкарпетка'];function close(){drawer.classList.remove('open');shade.classList.remove('open')}function show(x){home.classList.remove('active');ritual.classList.remove('active');x.classList.add('active');close();scrollTo(0,0);setTimeout(resize,30)}function n(){return parseInt(count.textContent.replace(/\s/g,''),10)||4351}function setN(v){count.textContent=new Intl.NumberFormat('uk-UA').format(v)}function note(t){toast.textContent=t;toast.classList.add('show');clearTimeout(window.t);window.t=setTimeout(()=>toast.classList.remove('show'),1800)}$('#goRitual').onclick=()=>show(ritual);$('#addEnergy').onclick=()=>{range.value=Math.min(100,+range.value+12);range.dispatchEvent(new Event('input'));setN(n()+1);note('Енергію підкинуто')};$('#random').onclick=()=>$('#punishment').value=punishments[Math.floor(Math.random()*punishments.length)];$('#ritualForm').onsubmit=e=>{e.preventDefault();setN(n()+1);note('Ритуал запущено')};menu.onclick=()=>{drawer.classList.toggle('open');shade.classList.toggle('open')};shade.onclick=close;document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>show(b.dataset.go==='home'?home:ritual));range.oninput=()=>{energy=+range.value/100;stage.style.setProperty('--energy',energy.toFixed(2));if(video){video.playbackRate=.72+energy*.72;video.play().catch(()=>{})}};function resize(){const r=canvas.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);canvas.width=Math.round(r.width*d);canvas.height=Math.round(r.height*d);ctx.setTransform(d,0,0,d,0,0)}function frame(){const r=canvas.getBoundingClientRect(),w=r.width,h=r.height;ctx.clearRect(0,0,w,h);if(energy>.28&&Math.random()<energy*.32){const a=Math.random()*Math.PI*2,rad=w*(.18+Math.random()*.24);sparks.push({x:w*.5+Math.cos(a)*rad,y:h*.55+Math.sin(a)*rad*.75,vx:(Math.random()-.5)*18,vy:-35-Math.random()*65,l:0,m:.35+Math.random()*.55,s:.7+Math.random()*1.5})}sparks=sparks.filter(p=>{p.l+=.016;if(p.l>p.m)return false;p.x+=p.vx*.016;p.y+=p.vy*.016;ctx.globalAlpha=(1-p.l/p.m)*energy;ctx.fillStyle=Math.random()>.45?'#ffd36b':'#ff7b16';ctx.beginPath();ctx.arc(p.x,p.y,p.s,0,Math.PI*2);ctx.fill();return true});ctx.globalAlpha=1;requestAnimationFrame(frame)}video&&video.addEventListener('canplay',()=>video.play().catch(()=>{}));document.addEventListener('visibilitychange',()=>{if(!document.hidden&&video)video.play().catch(()=>{})});addEventListener('resize',resize,{passive:true});range.dispatchEvent(new Event('input'));resize();requestAnimationFrame(frame)})();