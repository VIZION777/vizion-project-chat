(()=>{const $=s=>document.querySelector(s),home=$('#home'),ritual=$('#ritual'),drawer=$('#drawer'),shade=$('#shade'),menu=$('#menuToggle'),count=$('#count'),range=$('#range'),stage=$('#fireStage'),canvas=$('#fireCanvas'),toast=$('#toast'),ctx=canvas.getContext('2d');let energy=+range.value/100,last=performance.now(),imgReady=false;const img=new Image();img.src='./assets/fire-reference.jpg';img.onload=()=>{imgReady=true;resize()};const punishments=['Щоб Wi-Fi ловив тільки біля роутера','Щоб кава завжди остигала за 30 секунд','Щоб зарядка зникала саме коли треба','Щоб будильник дзвонив у вихідний','Щоб після прання губилась одна шкарпетка'];function close(){drawer.classList.remove('open');shade.classList.remove('open')}function show(x){home.classList.remove('active');ritual.classList.remove('active');x.classList.add('active');close();scrollTo(0,0);setTimeout(resize,30)}function n(){return parseInt(count.textContent.replace(/\s/g,''),10)||4351}function setN(v){count.textContent=new Intl.NumberFormat('uk-UA').format(v)}function note(t){toast.textContent=t;toast.classList.add('show');clearTimeout(window.t);window.t=setTimeout(()=>toast.classList.remove('show'),1800)}$('#goRitual').onclick=()=>show(ritual);$('#addEnergy').onclick=()=>{range.value=Math.min(100,+range.value+12);range.dispatchEvent(new Event('input'));setN(n()+1);note('Енергію підкинуто')};$('#random').onclick=()=>$('#punishment').value=punishments[Math.floor(Math.random()*punishments.length)];$('#ritualForm').onsubmit=e=>{e.preventDefault();setN(n()+1);note('Ритуал запущено')};menu.onclick=()=>{drawer.classList.toggle('open');shade.classList.toggle('open')};shade.onclick=close;document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>show(b.dataset.go==='home'?home:ritual));range.oninput=()=>{energy=+range.value/100;stage.style.setProperty('--energy',energy)};function resize(){const r=canvas.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);canvas.width=Math.max(1,Math.round(r.width*d));canvas.height=Math.max(1,Math.round(r.height*d));ctx.setTransform(d,0,0,d,0,0)}function drawImageCover(image,w,h,scale=1,alpha=1){const ir=image.width/image.height,cr=w/h;let sw=image.width,sh=image.height,sx=0,sy=0;if(ir>cr){sw=image.height*cr;sx=(image.width-sw)/2}else{sh=image.width/cr;sy=(image.height-sh)/2}ctx.globalAlpha=alpha;ctx.drawImage(image,sx,sy,sw,sh,(w-w*scale)/2,(h-h*scale)/2,w*scale,h*scale);ctx.globalAlpha=1}function render(now){const r=canvas.getBoundingClientRect(),w=r.width,h=r.height;if(!w||!h){requestAnimationFrame(render);return}ctx.clearRect(0,0,w,h);ctx.fillStyle='#000';ctx.fillRect(0,0,w,h);if(imgReady){const t=now/1000;const e=energy;const slices=64;const sliceH=h/slices;const ir=img.width/img.height,cr=w/h;let sw=img.width,sh=img.height,sx=0,sy=0;if(ir>cr){sw=img.height*cr;sx=(img.width-sw)/2}else{sh=img.width/cr;sy=(img.height-sh)/2}for(let i=0;i<slices;i++){const y=i*sliceH;const ny=i/slices;const heat=Math.max(0,1-ny);const amp=(1.5+e*8.5)*(0.28+heat*.72);const dx=Math.sin(t*(2.3+e*2.2)+i*.41)*amp+Math.sin(t*4.7+i*.17)*amp*.35;const rise=Math.sin(t*(1.7+e*1.8)+i*.22)*(e*1.8);const sy0=sy+(sh/slices)*i;const sh0=sh/slices+1;ctx.globalAlpha=.72+.28*e;ctx.drawImage(img,sx,sy0,sw,sh0,dx,y-rise,w,sliceH+1)}ctx.globalCompositeOperation='screen';ctx.globalAlpha=.16+.34*e;for(let i=0;i<36;i++){const y=i*(h/36);const dx=Math.sin(t*5.2+i*.73)*(2+e*7);ctx.drawImage(canvas,0,y,w,h/36,dx-1,y-2,w+2,h/36+4)}ctx.globalCompositeOperation='source-over';ctx.globalAlpha=1;const glow=ctx.createRadialGradient(w*.5,h*.52,20,w*.5,h*.52,w*.46);glow.addColorStop(0,'rgba(255,95,0,'+(0.04+e*.10)+')');glow.addColorStop(.55,'rgba(255,40,0,'+(0.02+e*.06)+')');glow.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=glow;ctx.fillRect(0,0,w,h)}requestAnimationFrame(render)}addEventListener('resize',resize,{passive:true});range.dispatchEvent(new Event('input'));resize();requestAnimationFrame(render)})();